# fontokmai — กติกาถาวรสำหรับ Claude

อ่าน [AGENTS.md](AGENTS.md) ทั้งไฟล์ก่อนเริ่มงานทุกครั้ง ไฟล์นี้เป็นช่องทางสื่อสารหลักกับ Codex และผู้ใช้ และถูกจำกัดให้สั้นกว่า 32 KiB
จากนั้นอ่านเฉพาะหัวข้อที่เกี่ยวกับงานใน [docs/design/fontokmai-design.md](docs/design/fontokmai-design.md) (แบบระบบ) และ [docs/sources.md](docs/sources.md) (แหล่งข้อมูลและสิทธิ์)
ประวัติเต็มอยู่ใน `private/handoffs/` (private repo ไม่อยู่ใน repo สาธารณะ) ซึ่งเป็นประวัติ ไม่ใช่สถานะปัจจุบัน

- ดูสถานะโปรเจกต์ที่หัวไฟล์ AGENTS.md ก่อนเสมอ
- ก่อนแก้ไฟล์ให้จองงานใน A5 และเมื่อเสร็จให้ลบแถวที่จองแล้วเพิ่มบันทึกบนสุดของ A6 (ทำอะไร, ไฟล์, ผลทดสอบจริง, ข้อจำกัด, ขั้นต่อไป) โดยเก็บ A6 ไว้ไม่เกิน 5 รายการ
- **ฟรีเท่านั้น**: ห้ามเสนอหรือเพิ่มสิ่งที่มีค่าใช้จ่ายหรือต้องผูกบัตร โดยไม่ถามผู้ใช้ก่อน
- ตอบผู้ใช้เป็นภาษาไทยเมื่อผู้ใช้เขียนภาษาไทย ใช้ภาษาง่าย ไม่ใช้ศัพท์เทคนิคโดยไม่อธิบาย ส่วนโค้ด คอมเมนต์ และ commit ใช้ภาษาอังกฤษ
- เวลาใช้ ICT (UTC+7) และวันที่แบบ ISO
- ห้าม commit `.env`, key/token, ข้อมูลดิบขนาดใหญ่ หรือ `.claude/` และห้ามสมัครบัญชีหรือส่งคำขอภายนอกแทนผู้ใช้
- ประกาศทางการต้องแยกจากค่าที่ระบบคำนวณเองเสมอ และทุกค่าที่แสดงต้องบอกแหล่งที่มากับเวลาข้อมูล
